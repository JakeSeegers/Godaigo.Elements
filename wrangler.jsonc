// Cloudflare Workers settings: publish the static game from dist/.
// dist/ is built by tools/build-site.js (the Workers Builds build command),
// which copies only the files the game loads.
{
  "name": "godaigo",
  "compatibility_date": "2026-09-01",
  "assets": {
    "directory": "./dist"
  }
}
