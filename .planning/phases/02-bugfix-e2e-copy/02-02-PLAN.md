# Plan 02-02: Tutorial Copy Improvements

**Phase:** 2 — Bug Fixes, E2E Verification & Copy
**Requirements:** COPY-01, COPY-02
**Status:** pending
**Can run in parallel with:** 02-01

---

## Task

Update tutorial step text in `tutorial-mode.js` to explicitly state the hand/active area limits and the per-type stone limits, so players aren't guessing at constraints.

## Files

- `js/tutorial-mode.js` — step text for scroll inventory step and stone collection step

## Action

1. Read `js/tutorial-mode.js` and locate the step covering "Scroll Inventory" (Hand vs Active Area explanation — current step 7)
2. Update text to explicitly include: `"Your Hand holds up to 2 scrolls. Your Active Area holds up to 2 scrolls. Scrolls move one way only — Active → Common pile."`
3. Locate the step covering stone collection (current step 9, or renumbered after shrine insertion)
4. Update text to explicitly include per-type limits: `"Each shrine holds a limited source pool: Earth 5 stones, Water 4, Fire 3, Wind 2, Void 1. The shared source pool holds max 25 stones total. You collect from this pool when you End Turn on a shrine center."`
5. Keep text concise — max 3 sentences per step; split into two text blocks if needed

## Verify

- [ ] Scroll inventory step text includes "max 2 per area" (or equivalent explicit number)
- [ ] Stone collection step text includes per-type limits (Earth=5, Water=4, Fire=3, Wind=2, Void=1)
- [ ] Stone collection step text includes the 25-cap source pool note
- [ ] No step text is longer than ~60 words
- [ ] Steps still display correctly in the tutorial overlay

## Done when

A player reading step 7 knows exactly how many scrolls they can hold, and reading the stone step knows the per-type and total limits.
