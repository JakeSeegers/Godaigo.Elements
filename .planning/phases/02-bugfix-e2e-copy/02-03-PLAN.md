# Plan 02-03: Full Tutorial End-to-End Playthrough Verification

**Phase:** 2 — Bug Fixes, E2E Verification & Copy
**Requirements:** E2E-01, E2E-02, E2E-03
**Status:** pending
**Depends on:** 02-01, 02-02 (fixes applied before verification)

---

## Task

Perform a full read-through of all 16 tutorial steps in `tutorial-mode.js` looking for dead ends, broken references, missing advance triggers, or incorrect step indices. Fix any issues found.

## Files

- `js/tutorial-mode.js` — all steps
- `js/game-core.js` — hooks and any referenced functions

## Action

1. Read the full steps array in `tutorial-mode.js` end-to-end
2. For each step, verify:
   - `spotlight` target element ID exists in `index.html` or is `null`/`'board'`
   - `blocking` mode is correct (should block when player must click a specific target, not block move/place steps)
   - Advance trigger is wired (Next button visible, or event hook exists)
   - Step text references valid game mechanics
3. Cross-check hook calls: `onPlayerTilePlaced`, `onTilePreReveal`, `onTileRevealed`, `onPlayerMoved`, `onEndTurn` — all must be called from game-core.js equivalent events
4. Check the tutorial exit path: confirm closing tutorial from any step calls page reload (existing behavior)
5. Fix any dead ends found (missing advance triggers, bad spotlight IDs, broken hook references)
6. Document what was verified and what was fixed in a VERIFICATION.md

## Verify

- [ ] All 16 step objects have a valid advance mechanism (Next button or event hook)
- [ ] All spotlight element IDs resolve (or are board/null)
- [ ] All 5 tutorial hooks are called from game-core.js
- [ ] Tutorial exit from step 1, step 8, and step 16 all reload page correctly
- [ ] VERIFICATION.md written in this phase directory with step-by-step results

## Done when

VERIFICATION.md confirms all 16 steps have a clear advance path and no broken references.
