# Plan 01-01: Add onEndTurn Hook to game-core.js

**Phase:** 1 — Earth Shrine Wiring
**Requirements:** WIRE-01
**Status:** pending

---

## Task

Add a tutorial hook call inside `endTurn()` in `js/game-core.js` so that `tutorial-mode.js` can intercept turn-end events during the tutorial.

## Files

- `js/game-core.js` — modify `endTurn()` function

## Action

1. Read `js/game-core.js` and locate the `endTurn()` function
2. Find the point after AP is reset and turn switches (but before any async Supabase calls)
3. Insert the hook call:

```js
if (window.isTutorialMode && window.TutorialMode?.onEndTurn) {
    window.TutorialMode.onEndTurn();
}
```

4. Verify the hook is placed so it fires for the LOCAL player's turn end only (not when the game processes the other side)

## Verify

- [ ] `endTurn()` contains the hook call
- [ ] Hook is guarded by `window.isTutorialMode` (no-op outside tutorial)
- [ ] No existing endTurn logic is broken (AP reset, turn counter, Supabase update all still run)
- [ ] No JS syntax errors (open file and scan)

## Done when

`window.TutorialMode.onEndTurn` is callable from within a tutorial session when the player clicks End Turn.
