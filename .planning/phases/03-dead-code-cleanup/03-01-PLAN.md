# Plan 03-01: Remove Dead tutorial.js Code

**Phase:** 3 — Dead Code Cleanup
**Requirements:** CLEAN-01
**Status:** pending

---

## Task

Delete `js/tutorial.js` (the deprecated 7-step modal tutorial) and remove its `<script>` tag from `index.html`. Verify no other code references its modal IDs or functions.

## Files

- `js/tutorial.js` — delete
- `index.html` — remove script tag
- Any other file referencing old modal IDs (search required)

## Action

1. Read `js/tutorial.js` to identify all global functions and DOM IDs it defines
2. Search the codebase for any references to those globals/IDs outside `tutorial.js` itself
3. If references exist in other files: remove them (they're dead references to a removed system)
4. Remove the `<script src="js/tutorial.js"></script>` line from `index.html`
5. Delete `js/tutorial.js`
6. Open browser and confirm no `404` or `Uncaught ReferenceError` on page load

## Verify

- [ ] `js/tutorial.js` is deleted
- [ ] `index.html` no longer contains `tutorial.js` script tag
- [ ] No other JS file calls functions from the old tutorial (grep for function names found in step 1)
- [ ] No old modal DOM IDs referenced in CSS or HTML
- [ ] Browser console is clean on page load (no 404s, no reference errors)

## Done when

Page loads cleanly with no trace of the old tutorial system.
